{
  "name": "swifttill-pos-monorepo",
  "version": "46.0.0",
  "private": true,
  "description": "SwiftTill POS V46: online-only business day open/close, after-midnight sales grouping, compact reports and print cleanup.",
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/print-agent",
    "packages/*"
  ],
  "scripts": {
    "start": "node apps/api/server.js",
    "dev": "node apps/api/server.js",
    "check": "node --check apps/api/server.js && node --check packages/db/src/client.js && node --check packages/db/src/adapter.js && node --check packages/auth/src/index.js && node --check packages/storage/src/r2.js && node --check apps/print-agent/agent.js",
    "check:web": "node -e \"new Function(require('fs').readFileSync('apps/web/public/assets/js/app.js','utf8')); console.log('web js ok')\"",
    "seed:reset": "node scripts/reset-local-db.js",
    "db:schema": "echo Prisma schema is in packages/db/prisma/schema.prisma",
    "validate": "npm run check && npm run check:web && npm run auth:smoke",
    "db:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "db:migrate": "prisma migrate deploy --schema packages/db/prisma/schema.prisma",
    "db:push": "prisma db push --schema packages/db/prisma/schema.prisma",
    "db:seed": "node packages/db/seed.js",
    "auth:smoke": "node packages/auth/smoke-test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8",
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5",
    "@aws-sdk/client-s3": "^3.637.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}