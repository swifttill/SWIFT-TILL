{
  "name": "@swifttill/print-agent",
  "version": "44.0.0",
  "private": true,
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "check": "node --check agent.js"
  },
  "description": "SwiftTill POS V44 safe Windows counter agent with local-only bind, diagnostics, spooling, retry, offline backup restore and atomic backups."
}
